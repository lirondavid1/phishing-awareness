

# CONTEXT

You already executed the integration prompt and produced a plan + applied it on a feature branch inside `ng-portals`. The seven conceptual contributions from the `poalimad` source were supposed to land in ng-portals' BMAD customization surface (`_bmad/custom/`, `docs/`, and two root-level `BANK-*.md` files).

I now want an independent verification pass. The goal is to confirm — strictly from the current state of the ng-portals working tree — that the integration actually landed correctly: every new file is present at the right path with the right content, every semantic addition is detectable inside its target file at the right location, no placeholder was substituted, no out-of-scope file was touched, and no TOML is broken.

This is a **read-only** pass. Do not modify any file. Do not run a merge or amend a commit. Do not push. If you find gaps, report them — do not fix them on your own initiative.

---

# WHAT TO VERIFY — THE SEVEN CONCEPTUAL CONTRIBUTIONS

Use these as your reference frame. Every check below must tie back to one of these seven items:

1. **Four-document owner-aligned standards split** under `docs/standards/`:
   - `regulation.md` (REG-*), `architecture-security.md` (ARCH-SEC-*), `code-security.md` (CODE-SEC-*), `code-quality.md` (CODE-QUAL-*)
   - Each agent loads only the documents its role requires.

2. **`bmad-security-design` Custom Skill** at `_bmad/custom/skills/bmad-security-design/` with three files: `SKILL.md`, `customize.toml`, `threat-modeling-examples.md`. `SKILL.md` must contain a "Threat-model lifecycle" section.

3. **`threat-model.md` as a HARD-REQUIRED artifact** at the implementation-readiness gate. The override `_bmad/custom/bmad-check-implementation-readiness.toml` CHECK 2B must FAIL on absence of `threat-model.md` and CONCERN on staleness.

4. **Per-role document loading** wired into the six bank-persona agent overrides:
   - Galit (PM) loads `regulation.md`
   - Yossi (Architect) loads `architecture-security.md`
   - Shraga (Dev / Builder) loads `code-security.md` + `code-quality.md`
   - Lenny (UX) loads `regulation.md` + `code-security.md`
   - Dvora (Analyst) loads `regulation.md`
   - Paige (Tech Writer) loads `regulation.md` + `code-security.md`

5. **Option X — mid-workflow dispatch** from `bmad-create-architecture` into `bmad-security-design`. The dispatch happens once, mid-workflow, before architecture finalization. The dead-loop guard `BMAD_CASCADE_FROM=security-design` must be absent. `on_complete` numbering should be 1 = readiness, 2 = promote-to-domain, 3 = recap.

6. **Audit-trail commit policy** at `docs/audit-trail-conventions.md`, referenced from `BANK-CUSTOMIZATIONS.md`.

7. **PRD `## Regulation` section discipline** — `docs/bank-prd-conventions.md` shows Regulation as section #7 (markdown bold), `docs/bank-prd-template.md` includes the section template, and `bmad-validate-prd.toml` raises a CONCERN if the section is missing.

---

# WHAT TO DO

Execute the seven verification blocks below in order. Each block is a runnable bash snippet. Stop and continue through every block even if one fails — report all failures at the end, not just the first.

For every check, report **PASS** or **FAIL** with the specific evidence (path, line number, grep output). At the end, produce a single summary table.

Run all commands from the BMAD root inside `ng-portals` (the path you identified in Step 2 of the integration prompt). All paths below are relative to that BMAD root.

## Block 1 — File existence (12 ADD files)

```bash
echo "=== Block 1: File existence ==="
for f in \
  "_bmad/custom/skills/bmad-security-design/SKILL.md" \
  "_bmad/custom/skills/bmad-security-design/customize.toml" \
  "_bmad/custom/skills/bmad-security-design/threat-modeling-examples.md" \
  "_bmad/custom/bmad-check-implementation-readiness.toml" \
  "_bmad/custom/bmad-code-review.toml" \
  "docs/audit-trail-conventions.md" \
  "docs/standards/regulation.md" \
  "docs/standards/architecture-security.md" \
  "docs/standards/code-security.md" \
  "docs/standards/code-quality.md" \
  "BANK-CHANGELOG.md" \
  "BANK-CUSTOMIZATIONS.md"; do
  if [ -f "$f" ]; then
    echo "  PASS: $f ($(wc -c < "$f") bytes)"
  else
    echo "  FAIL: $f is missing"
  fi
done
```

For each ADD file that is present, also report whether it appears to be the byte-exact poalimad content (e.g., the bank may have kept its own `BANK-CHANGELOG.md` and rejected the merge for that one — that's fine, just report it).

## Block 2 — Folder cardinality (loose, not strict)

```bash
echo "=== Block 2: Folder cardinality ==="
if [ -d "docs/standards" ]; then
  n=$(find docs/standards -maxdepth 1 -type f -name "*.md" | wc -l | tr -d ' ')
  echo "  docs/standards/ has $n .md files (expect at least 4: regulation, architecture-security, code-security, code-quality)"
fi
if [ -d "_bmad/custom/skills/bmad-security-design" ]; then
  n=$(find _bmad/custom/skills/bmad-security-design -maxdepth 1 -type f | wc -l | tr -d ' ')
  echo "  bmad-security-design/ has $n files (expect at least 3: SKILL.md, customize.toml, threat-modeling-examples.md)"
fi
```

The expected counts are LOWER BOUNDS, not equality. The bank may legitimately have additional standards docs or additional skill files; that's not a failure.

## Block 3 — TOML parse health

```bash
echo "=== Block 3: TOML parse health ==="
python3 - <<'PY'
import sys, pathlib
try:
    import tomllib
except ImportError:
    import tomli as tomllib
bad = []
for p in pathlib.Path("_bmad/custom").rglob("*.toml"):
    try:
        tomllib.loads(p.read_text(encoding="utf-8"))
    except Exception as e:
        bad.append((str(p), str(e)))
if bad:
    for f, e in bad: print(f"  FAIL: {f}: {e}")
    sys.exit(1)
print(f"  PASS: all {len(list(pathlib.Path('_bmad/custom').rglob('*.toml')))} TOML files parse cleanly")
PY
```

Every TOML must parse. If any fails, the merge introduced a syntax break and must be reported.

## Block 4 — Semantic presence per contribution

For each of the seven contributions, run the corresponding grep checks. Use `grep -qF` (fixed-string, no regex) to avoid escaping pitfalls.

### Contribution 1 — Four-doc split
```bash
echo "=== Block 4.1: Four-doc split ==="
for ruleprefix in "REG-" "ARCH-SEC-" "CODE-SEC-" "CODE-QUAL-"; do
  if grep -qF "$ruleprefix" docs/standards/*.md 2>/dev/null; then
    echo "  PASS: rule prefix $ruleprefix appears in docs/standards/"
  else
    echo "  FAIL: rule prefix $ruleprefix not found"
  fi
done
```

### Contribution 2 — bmad-security-design skill with lifecycle section
```bash
echo "=== Block 4.2: bmad-security-design skill ==="
grep -qF "Threat-model lifecycle" _bmad/custom/skills/bmad-security-design/SKILL.md \
  && echo "  PASS: SKILL.md contains 'Threat-model lifecycle' section" \
  || echo "  FAIL: 'Threat-model lifecycle' section not found in SKILL.md"
grep -qF "STRIDE-FE" _bmad/custom/skills/bmad-security-design/SKILL.md \
  && echo "  PASS: SKILL.md mentions STRIDE-FE" \
  || echo "  FAIL: STRIDE-FE not mentioned in SKILL.md"
```

### Contribution 3 — threat-model.md as HARD-REQUIRED at readiness gate
```bash
echo "=== Block 4.3: threat-model.md hard-required ==="
F="_bmad/custom/bmad-check-implementation-readiness.toml"
grep -qF "threat-model.md" "$F" \
  && echo "  PASS: readiness gate references threat-model.md" \
  || echo "  FAIL: readiness gate does not reference threat-model.md"
grep -qF "REQUIRED ARTIFACT MISSING" "$F" \
  && echo "  PASS: readiness gate has REQUIRED ARTIFACT MISSING class" \
  || echo "  FAIL: readiness gate missing the hard-fail class"
```

### Contribution 4 — Per-role document loading
```bash
echo "=== Block 4.4: Per-role document loading ==="
# Galit (PM) loads regulation.md
grep -qF "docs/standards/regulation.md" _bmad/custom/bmad-agent-pm.toml \
  && echo "  PASS: Galit (PM) loads regulation.md" \
  || echo "  FAIL: Galit (PM) does not load regulation.md"
# Yossi (Architect) loads architecture-security.md
grep -qF "docs/standards/architecture-security.md" _bmad/custom/bmad-agent-architect.toml \
  && echo "  PASS: Yossi (Architect) loads architecture-security.md" \
  || echo "  FAIL: Yossi (Architect) does not load architecture-security.md"
# Shraga (Dev) loads code-security.md
grep -qF "docs/standards/code-security.md" _bmad/custom/bmad-agent-dev.toml \
  && echo "  PASS: Shraga (Dev) loads code-security.md" \
  || echo "  FAIL: Shraga (Dev) does not load code-security.md"
# Lenny (UX) loads regulation.md + code-security.md
grep -qF "docs/standards/regulation.md" _bmad/custom/bmad-agent-ux-designer.toml \
  && grep -qF "docs/standards/code-security.md" _bmad/custom/bmad-agent-ux-designer.toml \
  && echo "  PASS: Lenny (UX) loads regulation.md + code-security.md" \
  || echo "  FAIL: Lenny (UX) does not load both regulation.md and code-security.md"
# Dvora (Analyst) loads regulation.md
grep -qF "docs/standards/regulation.md" _bmad/custom/bmad-agent-analyst.toml \
  && echo "  PASS: Dvora (Analyst) loads regulation.md" \
  || echo "  FAIL: Dvora (Analyst) does not load regulation.md"
# Paige (Tech Writer) loads regulation.md + code-security.md
grep -qF "docs/standards/regulation.md" _bmad/custom/bmad-agent-tech-writer.toml \
  && grep -qF "docs/standards/code-security.md" _bmad/custom/bmad-agent-tech-writer.toml \
  && echo "  PASS: Paige (TechWr) loads regulation.md + code-security.md" \
  || echo "  FAIL: Paige (TechWr) does not load both regulation.md and code-security.md"
```

### Contribution 5 — Option X mid-workflow dispatch
```bash
echo "=== Block 4.5: Option X dispatch ==="
F="_bmad/custom/bmad-create-architecture.toml"
grep -qF "bmad-security-design" "$F" \
  && echo "  PASS: bmad-create-architecture dispatches bmad-security-design" \
  || echo "  FAIL: bmad-create-architecture does not dispatch bmad-security-design"
! grep -qF "BMAD_CASCADE_FROM=security-design" "$F" \
  && echo "  PASS: dead loop guard absent (G06)" \
  || echo "  FAIL: dead loop guard BMAD_CASCADE_FROM=security-design still present"
# on_complete numbering
grep -q "^STEP 1 — IMPLEMENTATION READINESS"   "$F" && echo "  PASS: STEP 1 = IMPLEMENTATION READINESS" || echo "  INFO: STEP 1 differs (bank may have own numbering)"
grep -q "^STEP 2 — FOUR-WAY PROMOTE-TO-DOMAIN" "$F" && echo "  PASS: STEP 2 = FOUR-WAY PROMOTE-TO-DOMAIN" || echo "  INFO: STEP 2 differs"
grep -q "^STEP 3 — RECAP"                      "$F" && echo "  PASS: STEP 3 = RECAP" || echo "  INFO: STEP 3 differs"
```

Treat the STEP-numbering checks as INFO not FAIL — the bank may have legitimately renumbered to fit their own `on_complete` chain.

### Contribution 6 — Audit-trail commit policy
```bash
echo "=== Block 4.6: Audit-trail commit policy ==="
[ -f "docs/audit-trail-conventions.md" ] \
  && echo "  PASS: docs/audit-trail-conventions.md exists" \
  || echo "  FAIL: docs/audit-trail-conventions.md missing"
grep -qF "docs/audit-trail-conventions.md" BANK-CUSTOMIZATIONS.md 2>/dev/null \
  && echo "  PASS: BANK-CUSTOMIZATIONS.md links audit-trail-conventions.md" \
  || echo "  INFO: BANK-CUSTOMIZATIONS.md does not link audit-trail-conventions.md (only matters if the bank merged our BANK-CUSTOMIZATIONS.md)"
```

### Contribution 7 — PRD ## Regulation section discipline
```bash
echo "=== Block 4.7: PRD ## Regulation section ==="
grep -qE "^7\.\s+\*\*Regulation\*\*" docs/bank-prd-conventions.md \
  && echo "  PASS: Regulation listed as section #7 (markdown bold)" \
  || echo "  FAIL: Regulation not at position #7"
grep -q "^## Regulation" docs/bank-prd-template.md \
  && echo "  PASS: bank-prd-template.md includes ## Regulation section" \
  || echo "  FAIL: bank-prd-template.md missing ## Regulation"
grep -qF "docs/standards/regulation.md" _bmad/custom/bmad-validate-prd.toml \
  && echo "  PASS: bmad-validate-prd loads regulation.md" \
  || echo "  FAIL: bmad-validate-prd does not load regulation.md"
```

## Block 5 — Placeholder integrity (no committee tokens were substituted)

```bash
echo "=== Block 5: Placeholder integrity ==="
for token in "[BANK-PLACEHOLDER]" "[REG-PLACEHOLDER]" "[ARCH-SEC-PLACEHOLDER]" "[CODE-SEC-PLACEHOLDER]" "[CODE-QUAL-PLACEHOLDER]"; do
  count=$(grep -rF "$token" docs/standards/ 2>/dev/null | wc -l | tr -d ' ')
  if [ "$count" -gt 0 ]; then
    echo "  PASS: $token still present ($count occurrences) — committee fill pending as expected"
  else
    echo "  FAIL: $token has zero occurrences — was it substituted, or are the standards docs missing?"
  fi
done
```

If any placeholder token has zero occurrences, either the file is missing or someone substituted real text into it. Both are FAIL conditions — bank committees own that substitution and you weren't supposed to touch it.

## Block 6 — Reference hygiene (no legacy / family-specific content)

```bash
echo "=== Block 6: Reference hygiene ==="
! grep -rqn "docs/security-standards.md" _bmad/custom/ \
  && echo "  PASS: no references to legacy docs/security-standards.md" \
  || { echo "  FAIL: stale references to docs/security-standards.md found:"; grep -rn "docs/security-standards.md" _bmad/custom/; }
! grep -rqn "docs/domains/family" _bmad/custom/ \
  && echo "  PASS: no references to docs/domains/family/" \
  || { echo "  FAIL: stale family-domain references found:"; grep -rn "docs/domains/family" _bmad/custom/; }
! grep -rqEni "child[- ]data|CHILD-(PII|DATA|STORAGE)" _bmad/custom/ \
  && echo "  PASS: no CHILD-* rule IDs leaked" \
  || { echo "  FAIL: CHILD-* references found:"; grep -rEn "child[- ]data|CHILD-(PII|DATA|STORAGE)" _bmad/custom/; }
```

## Block 7 — Out-of-scope change check (BMAD-managed trees + ng-portals app code untouched)

```bash
echo "=== Block 7: Out-of-scope change check ==="
# Compare the feature branch to the branch it was created from (substitute the parent branch name if different).
PARENT_BRANCH="${PARENT_BRANCH:-main}"
echo "  (comparing current branch to $PARENT_BRANCH)"

# Any changes inside BMAD-managed trees?
git diff --name-only "$PARENT_BRANCH"...HEAD -- '_bmad/core/' '_bmad/bmm/' '_bmad/bmb/' '_bmad/scripts/' \
  > /tmp/oos_bmad_managed.txt
if [ -s /tmp/oos_bmad_managed.txt ]; then
  echo "  FAIL: changes detected inside BMAD-managed trees:"
  cat /tmp/oos_bmad_managed.txt | sed 's/^/    /'
else
  echo "  PASS: BMAD-managed trees untouched"
fi

# Any changes outside the in-scope surface? In-scope = _bmad/custom/, docs/, BANK-CHANGELOG.md, BANK-CUSTOMIZATIONS.md.
git diff --name-only "$PARENT_BRANCH"...HEAD \
  | grep -vE '^(_bmad/custom/|docs/|BANK-CHANGELOG\.md$|BANK-CUSTOMIZATIONS\.md$)' \
  > /tmp/oos_other.txt
if [ -s /tmp/oos_other.txt ]; then
  echo "  FAIL: changes outside the in-scope surface:"
  cat /tmp/oos_other.txt | sed 's/^/    /'
else
  echo "  PASS: no changes outside _bmad/custom/ + docs/ + the two BANK-*.md files"
fi
```

If the parent branch isn't `main`, set `PARENT_BRANCH=...` before running. The point of this block is: any file Kiro modified that is NOT one of the 28 items in the integration plan is a side-effect that needs to be explained or reverted.

---

# OUTPUT FORMAT I WANT BACK

After running all seven blocks, give me ONE structured report with these sections:

## Summary table

A table with columns: Block | Contribution | Status (PASS / FAIL / INFO) | Evidence (short).

## Detailed failures (FAIL or INFO)

For every FAIL or INFO row in the summary, give me one paragraph that includes:
- The file and line where the gap is (or the absence of evidence)
- Whether it's a missed addition (Kiro forgot to apply it), an out-of-scope change (Kiro changed something unrelated), a conflict that wasn't reported earlier, or a bank-side intentional divergence (the bank has its own version that doesn't match ours)
- Your suggested resolution — but **do not act on the suggestion**. I'll decide.

## Side-effect check

If Block 7 surfaced changes outside the in-scope surface, give me the full list and your best guess for why each change happened.

## Recommendation

One paragraph: should I (a) accept the merge as-is, (b) ask you to apply specific follow-up fixes, or (c) roll back and re-plan? Justify briefly.

# IMPORTANT RULES

- This pass is read-only. Do not modify any file. Do not run a merge, amend, rebase, or push.
- Do not paraphrase the grep checks — they're designed to detect distinctive substrings without ambiguity. Use them verbatim.
- If a check fails because the expected file is missing entirely, that's a FAIL, not an INFO — report it as such and let me decide if the bank intentionally rejected the ADD.
- If a check passes but the surrounding context looks suspicious (e.g., the file exists but is empty, or the persistent_facts array has our entry but is malformed elsewhere), flag it as an INFO with detail.
- The TOML parse check (Block 3) is non-negotiable. If any TOML fails to parse, the working tree should NOT be merged into the bank's stable branch. Surface this prominently.

When you're ready, begin with Block 1.
